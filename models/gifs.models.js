const bcrypt = require("bcrypt");
const pool = require("../config/mysql.conf");

// return { id: val.id, title: val.title, url: val.images.original.url };

async function getGifs (res, username, password) {}

module.exports.getGifs = getGifs;